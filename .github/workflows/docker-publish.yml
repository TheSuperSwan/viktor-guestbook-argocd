name: Build and Push Guestbook Containers

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and push backend
        run: |
          docker build \
            -t ghcr.io/thesuperswan/guestbook-backend:latest \
            -f guestbook/guestbook-backend/Dockerfile.txt \
            .
          docker push ghcr.io/thesuperswan/guestbook-backend-argocd:latest
      - name: Build and push frontend
        run: |
          docker build \
            -t ghcr.io/thesuperswan/guestbook-frontend:latest \
            -f guestbook/guestbook-frontend/Dockerfile.txt \
            .
          docker push ghcr.io/thesuperswan/guestbook-frontend-argocd:latest
