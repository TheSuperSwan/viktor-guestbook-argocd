apiVersion: apps/v1
kind: Deployment
metadata:
  name: viktor-guestbook-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: viktor-guestbook-frontend
  template:
    metadata:
      labels:
        app: viktor-guestbook-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/thesuperswan/guestbook-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: viktor-guestbook-frontend
spec:
  selector:
    app: viktor-guestbook-frontend
  ports:
    - name: http
      port: 8080
      targetPort: 8080

